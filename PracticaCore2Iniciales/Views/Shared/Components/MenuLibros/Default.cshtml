@model List<Genero>
    <a class="nav-link dropdown-toggle" href="#" id="dropdown03" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Generos</a>
    <div class="dropdown-menu" aria-labelledby="dropdown03">
        @foreach (Genero genero in Model)
        {
            <a class="dropdown-item" 
               asp-controller="Libros"
               asp-action="LibrosGenero"
               asp-route-idgenero="@genero.IdGenero"
               >@genero.Nombre
        </a>
        }


    </div>
